import { NasRequest } from './nas.request'
import { NasSharesRequest } from './nas-shares.request'
import { NasPoolsRequest } from './nas-pools.request'
import { NasType } from './nas-type.enum'

export const nas = Object.freeze({
  NasRequest,
  NasSharesRequest,
  NasPoolsRequest,
  NasType,
})
